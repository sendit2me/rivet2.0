- Согда создаешь новый проект, в нем нет ни одного графа. Но если джобавить нод на форкспейс а потом создать новый граф, то окажется что созданныйц но сужествует в графе и вот уже показываетс ядва графа

- When the node has read a lot of data (large base64 image for example), it makes the app laggy and slow.

- В коде и джейсон темплейтах давать сворачивать код

- Кнопка копирования вывода нода копирует то то что ты видишь то внутренне представление Ривета (когда объекты). Надо бы унифицировать можеть быть?

- Сделать так, чтобы  Ctrl+Z работал и никогда не ломал граф. Иногда ломает, удаляет соединения