{
  "name": "@valerypopoff/rivet2-native-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "files": [
    "index.js",
    "native/**"
  ],
  "scripts": {
    "build:native": "cargo build --manifest-path native/Cargo.toml --release",
    "test:native": "cargo test --manifest-path native/Cargo.toml && npm run build:native && node test-native-runtime.mjs"
  }
}
