// eslint-disable-next-line import/no-cycle -- The plugin implementation uses GraphProcessor to execute local Gentrace tests.
import { gentracePlugin } from './plugin.js';

export default gentracePlugin;
